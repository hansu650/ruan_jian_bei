# 第十阶段说明：PracticeAgent + EvaluatorAgent 学习效果评估

## 1. 阶段目标

第十阶段实现 A3 赛题可选加分项“学习效果评估”：系统根据学习画像、学习路径、课程知识库和已生成资源生成练习测验，学生提交答案后自动批改，系统分析薄弱点、更新掌握度并推荐补救资源。

## 2. A3 题面对应

A3 要求系统能够跟踪练习测试情况、资源使用反馈等数据，并实现对学生学习效果的多维度评估。本阶段完成从“练习测验”到“批改反馈”再到“画像掌握度更新”的基础闭环。

## 3. PracticeAgent 设计

PracticeAgent 输入：

- Student
- Course
- LearnerProfile
- LearningPathStep
- DocumentChunk 检索片段
- GeneratedResource 辅助上下文

PracticeAgent 输出：

- PracticeQuiz
- PracticeQuestion 列表
- source_chunk_ids_json
- source_resource_ids_json

支持题型：

- `single_choice`
- `multiple_choice`
- `short_answer`
- `sql_practice`

题目为原创生成或模板生成，不复制教材原文。

## 4. EvaluatorAgent 设计

EvaluatorAgent 输入：

- PracticeQuiz
- PracticeQuestion
- 学生提交答案
- LearnerProfile.mastery_json

EvaluatorAgent 输出：

- PracticeAttempt
- PracticeAnswer
- weak_points
- strengths
- recommended_actions
- mastery_before_json
- mastery_after_json
- LearningEvaluationReport

## 5. 数据模型

- `PracticeQuiz`：一次练习测验。
- `PracticeQuestion`：测验题目，不在普通题目接口中暴露 `correct_answer_json`。
- `PracticeAttempt`：学生一次提交和批改结果。
- `PracticeAnswer`：逐题批改结果、得分和错因。
- `LearningEvaluationReport`：学习效果评估报告。

## 6. 练习生成流程

学习路径 step → PracticeAgent → MockLLM → fallback 题库 → 保存 Quiz 和 Questions。

如果 MockLLM 返回不可解析 JSON，系统会使用固定原创模板生成题目，保证无 API Key 也能演示。

## 7. 自动批改流程

学生提交答案 → EvaluatorAgent → 批改 → 错因分析 → 更新掌握度 → 生成 EvaluationReport。

批改规则：

- 单选题：严格匹配。
- 多选题：完全一致满分，部分命中给部分分。
- 简答题：按关键词覆盖程度评分。
- SQL 实操题：只检查 SQL 关键词，不执行 SQL。

## 8. 掌握度更新规则

- 正确率 >= 0.8：对应知识点掌握度 +8，最高 100。
- 正确率 >= 0.5 且 < 0.8：掌握度 +3。
- 正确率 < 0.5：掌握度 -5，最低 0。
- 不清空画像其他字段。
- LearnerProfile.version 自增。

## 9. 补救资源推荐

系统根据薄弱知识点生成补救建议，例如：

- 复习事务隔离级别图解讲义。
- 完成 B+ 树索引实操案例。
- 对 JOIN 错题进行二次练习。

本阶段只推荐补救方向，不自动重排学习路径。

## 10. 前端页面

### `/practice`

- 选择学生、课程、学习路径 step。
- 选择题型、难度和题目数量。
- 生成练习。
- 填写答案。
- 提交并展示批改结果、错因、掌握度变化。

### `/analytics`

- 展示 quiz_count、attempt_count、average_accuracy。
- 展示最新 mastery。
- 展示最新薄弱点和补救建议。
- 展示 LearningEvaluationReport 列表。

## 11. API 列表

- `GET /api/practice/question-types`
- `POST /api/practice/quizzes/generate`
- `GET /api/practice/quizzes`
- `GET /api/practice/quizzes/{quiz_id}`
- `POST /api/evaluation/attempts/submit`
- `GET /api/evaluation/attempts`
- `GET /api/evaluation/attempts/{attempt_id}`
- `GET /api/evaluation/reports`
- `GET /api/evaluation/reports/{report_id}`
- `GET /api/evaluation/analytics`

## 12. 本阶段没有实现

- 真实讯飞 API
- 防作弊
- 班级考试系统
- 用户登录
- 复杂报表
- 路径动态重排
- Docker

## 13. 版权边界

- 不复制教材原文。
- 不处理教材 PDF。
- 题目为原创生成或模板生成。
- 不输出出版社页码等未被引用支撑的来源。

## 14. 验收方式

```powershell
cd apps/api
pytest
ruff check .
mypy app tests

cd ../web
pnpm lint
pnpm typecheck

cd ../..
python scripts/check-env.py
.\scripts\check-phase10.ps1
```

## 15. 下一阶段计划

第十一阶段：系统收尾、演示流程、文档、PPT 大纲和视频脚本。

## Phase 10.1 衔接

在进入交付材料阶段前，项目先补充讯飞星火 HTTP Provider 可选接入：

- 新增 `SparkHTTPProvider`，使用讯飞星火 HTTP Chat Completions APIPassword 方式。
- 默认仍使用 MockLLM，保证没有 API Key 时也能演示第十阶段完整闭环。
- 自动化测试不真实调用讯飞 API，只通过 mock `httpx` 验证请求体、错误处理和密钥不泄露。
- `/llm-lab` 会显示 `spark_http_configured`，用于确认本地是否配置了讯飞星火 APIPassword。

Phase 10.1 不改变 PracticeAgent 和 EvaluatorAgent 的业务边界，不引入真实付费调用测试，不进入 PPT、视频或最终交付材料阶段。
# Phase 11 衔接说明

第十阶段和 Phase 10.1 完成后，项目先进入 Phase 11：端到端演示工作台与稳定性打磨，而不是直接进入最终交付材料阶段。

Phase 11 新增 `/api/demo/status`、`/api/demo/bootstrap` 和 `/demo` 演示工作台。演示工作台会检查第十阶段形成的完整闭环状态，包括练习测验、自动批改和学习效果评估，但不会自动生成测验或提交答案，也不会自动调用 Spark。
